import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Vibrant Blue (Logo Primary) - Main action buttons, active states
  static const Color primary = Color(0xFF00A3FF);
  static const Color accent = Color(0xFF00A3FF);

  // Vibrant Blue Dark - Dark mode primary, header/footer backgrounds
  static const Color primaryDark = Color(0xFF005C99);

  // Vibrant Blue Soft - Highlight backgrounds, secondary buttons
  static const Color primarySoft = Color(0xFFE0F7FF);
  static const Color accentSoft = Color(0xFFE0F7FF);

  // Bold Violet (Logo Accent) - Secondary actions, synergy features
  static const Color violet = Color(0xFF6200EE);
  static const Color secondary = Color(0xFF6200EE);

  // Violet Soft - Complementary highlights, background tints
  static const Color violetSoft = Color(0xFFE0CCFF);

  // Clean Cool Grey - Main app background
  static const Color background = Color(0xFFEFF0F2);

  // Pure White - Card backgrounds, list items
  static const Color surface = Color(0xFFFFFFFF);

  // Pure Black / Deep Grey - Main text color
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF000000);

  // Simple Grey - Divider lines, input borders
  static const Color border = Color(0xFFCCCCCC);
  static const Color disabled = Color(0xFFB8B2C2);
}
